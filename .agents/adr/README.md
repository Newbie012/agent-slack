# ADRs

ADRs record durable technical choices.

Use an ADR for stack, architecture, integration style, or test infrastructure decisions future contributors should not relitigate.

Behavior belongs in `.agents/prd/`. Implementation slices belong in `.agents/issues/`.

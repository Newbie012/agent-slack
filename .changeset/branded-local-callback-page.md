---
"@eliya-oss/agent-slack": patch
---

The local OAuth callback page shown in the browser after `agent-slack auth login` is now branded instead of an unstyled line of text. Success and failure both render a dark, centered page matching the hosted relay, and the failure page now tells you to return to the terminal and retry.
